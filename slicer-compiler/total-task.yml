- name: compiling nfv-ifa-libs
  shell: cd {{ ansible_env.HOME }}/slicer-merge/nfv-ifa-libs && mvn clean install

- name: compiling identity-management
  shell: cd {{ ansible_env.HOME }}/slicer-merge/identity-management && mvn clean install

- name: compiling nfv-sol-libs
  shell: cd {{ ansible_env.HOME }}/slicer-merge/nfv-sol-libs && ./install_nfv_sol_libs.sh

############ to be changed ###########
- name: compiling OSMr4PlusClient
  shell: cd {{ ansible_env.HOME }}/5g-catalogue/OSMr4PlusDataModel && mvn clean install

- name: compiling nfvo-driver
  shell: cd {{ ansible_env.HOME }}/slicer-merge/nfvo-driver && mvn clean install
#######################################

- name: compiling blueprint-catalogue
  shell: cd {{ ansible_env.HOME }}/slicer-merge/blueprint-catalogue && mvn clean install

- name: compiling 5geve rest client
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/5GEVE_REST_CLIENTS && mvn clean install

- name: compiling record service im
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/RecordServiceIM && mvn clean install

- name: compiling record services
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/RecordService && mvn clean install

- name: compiling common_element
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/CommonElements && mvn clean install

- name: compiling arbitrator interfaces
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/ARBITRATOR_INTERFACES && mvn clean install

- name: compiling nsfm interfaces
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/NSFM_INTERFACES && mvn clean install

- name: compiling arbitrator service
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/ARBITRATOR_SERVICE && mvn clean install

- name: compiling vsmf_interfaces
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/VSFM_INTERFACES && mvn clean install

- name: compiling vsmf_services
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/VSFM_SERVICE && mvn clean install

- name: compiling nsfm service
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/NSFM_SERVICE && mvn clean install

- name: compiling nsfm service
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/NFVO_CATALOGUE_REST && mvn clean install

- name: compiling sebastian core
  shell: cd {{ ansible_env.HOME }}/slicer-merge/slicer/SEBASTIAN/SEBASTIAN_CORE && mvn clean install